# Network_Simulation_DVRP
A decentralized Distance Vector Routing implementation that simulates a self-healing network. Nodes act as independent routers, discovering the most efficient data paths across a dynamic topology using a weighted Bellman-Ford algorithm over UDP.
